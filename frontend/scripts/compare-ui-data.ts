import * as fs from 'fs';
import * as path from 'path';

// A small utility script to diff legacy frontend JSON with API response JSON
// In Next.js this can just be run with ts-node.

function main() {
  console.log('Comparison Tool Ready.');
  console.log('Usage: Load legacy JSON and new API JSON, then diff key fields like totalAmount and statuses.');
}

main();
