const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient({datasources: { db: { url: 'postgresql://himalaya_erp_user:CHANGE_ME_TO_A_STRONG_PASSWORD@localhost:5433/himalaya_erp?schema=public' }}}); async function main() { const users = await prisma.user.findMany({where: {email: {contains: 'store'}}}); console.log(users.map(u => ({email: u.email, pass: u.password}))); } main().finally(() => prisma.$disconnect());
