const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const u = await prisma.user.findUnique({where: {email: 'store.manager@himalayaerp.com'}}); console.log(u ? 'User exists' : 'User not found'); if(u) { const bcrypt = require('bcrypt'); const isValid = await bcrypt.compare('admin123', u.password); console.log('Password valid:', isValid); console.log('isActive:', u.isActive); } } main().finally(() => prisma.$disconnect());
