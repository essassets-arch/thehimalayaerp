const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient({datasources: { db: { url: 'postgresql://himalaya_erp_user:CHANGE_ME_TO_A_STRONG_PASSWORD@localhost:5433/himalaya_erp?schema=public' }}}); async function main() { console.log('Products:', await prisma.product.count()); console.log('RawMaterials:', await prisma.rawMaterial.count()); console.log('Transactions:', await prisma.inventoryTransaction.count()); console.log('GRNs:', await prisma.goodsReceiptNote.count()); console.log('MatReq:', await prisma.materialRequest.count()); } main().finally(() => prisma.$disconnect());
